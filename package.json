{
	"name": "electrobun-vanilla-vite",
	"version": "1.0.0",
	"type": "module",
	"description": "Electrobun app with vanilla TypeScript and Vite HMR",
	"scripts": {
    	"start": "bun run build:all && NODE_ENV=production electrobun dev",
		"build:all": "vite build && VITE_VIEW=keybinds-helper VITE_APPLET=true vite build",
    	"dev:hmr": "concurrently \"bun run hmr\" \"NODE_ENV=development electrobun dev\"",
    	"hmr": "VITE_VIEW=bar-ui vite --port 5173",
    	"build:canary": "bun run build:all && electrobun build --env=canary",
		"refresh": "bun scripts/refresh.ts"
	},
	"dependencies": {
		"electrobun": "1.15.1"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"concurrently": "^9.1.0",
		"typescript": "^5.7.2",
		"vite": "^6.0.3"
	}
}
